#include <stdio.h>
/**
 * main - program that prints the fibonacci series of 50
 * Return: 0
 */
int main(void)
{
	int a = 0;
	int b = 1;
	for (i = 0; i < 50; i++)
	{
		a++
		b++
		if (i !=50)
		{
			printf("%d, ", a+b);
		}
	}
	printf("\n")
	return (0);
}
